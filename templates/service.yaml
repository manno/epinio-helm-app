apiVersion: v1
kind: Service
metadata:
  name: {{ include "epinio-app.fullname" . }}
  labels:
    {{- include "epinio-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "epinio-app.selectorLabels" . | nindent 4 }}
